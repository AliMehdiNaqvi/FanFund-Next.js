/** @type {import('next').NextConfig} */
const nextConfig = {
  // images: {
  //   domains: ['images.unsplash.com', 'img.freepik.com', 'mir-s3-cdn-cf.behance.net','cdn-icons-png.flaticon.com'],
  // },
};

export default nextConfig;